import React from "react";
import ReactDOM from "react-dom/client";
import DiagnosisApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DiagnosisApp />
  </React.StrictMode>
);
